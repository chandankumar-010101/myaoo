class ScreenArguments {
  final String name;
  final String location;
  final String profile_pic;
  final String rating;
  final String students;
  final String source_language;
  final String target_language;
  final String about;

  ScreenArguments(this.name, this.location, this.profile_pic, this.rating,
      this.students, this.source_language, this.target_language, this.about);

  // ScreenArguments(this.title, this.message);
}
